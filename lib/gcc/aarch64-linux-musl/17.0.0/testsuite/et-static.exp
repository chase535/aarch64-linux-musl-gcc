# This file is auto-generated by gen-et-static.awk from auto-host.h
# and et-static.def.

# Return 1 if your assembler and linker support calling ___tls_get_addr via GOT, 0 otherwise.
proc check_effective_target_tls_get_addr_via_got { } {
    return 0
}

# Return 1 if this target supports SHT_INIT_ARRAY, 0 otherwise.
proc check_effective_target_sht_init_array { } {
    return 1
}

# Return 1 if this target supports SHF_GNU_RETAIN, 0 otherwise.
proc check_effective_target_shf_gnu_retain { } {
    return 1
}

# Return 1 if your assembler supports htm insns on power10, 0 otherwise.
proc check_effective_target_powerpc_as_p10_htm { } {
    return 0
}

# Return 1 if -mfentry is enabled by default, 0 otherwise.
proc check_effective_target_fentry { } {
    return 0
}

# Return 1 if using the Solaris linker, 0 otherwise.
proc check_effective_target_solaris_ld { } {
    return 0
}

# Return 1 if your assembler and linker support @GOT, 0 otherwise.
proc check_effective_target_got32x_reloc { } {
    return 0
}

# Return 1 if your linker supports GNU style response files, 0 otherwise.
proc check_effective_target_ld_at_file { } {
    return 1
}

# Return 1 if using the Solaris assembler, 0 otherwise.
proc check_effective_target_solaris_as { } {
    return 0
}

# Return 1 if this target supports SHF_LINK_ORDER, 0 otherwise.
proc check_effective_target_shf_link_order { } {
    return 1
}

# Return 1 if your linker supports -pie option with copy reloc, 0 otherwise.
proc check_effective_target_pie_copyreloc { } {
    return 0
}

# Return 1 if assembler and linker support R_X86_64_CODE_6_GOTTPOFF, 0 otherwise.
proc check_effective_target_code_6_gottpoff_reloc { } {
    return 0
}
